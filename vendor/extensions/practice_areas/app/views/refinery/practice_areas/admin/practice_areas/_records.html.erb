<%= render 'refinery/admin/search_header', :url => refinery.practice_areas_admin_practice_areas_path %>
<div class='pagination_container'>
  <% if @practice_areas.any? %>
    <%= render 'practice_areas' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
